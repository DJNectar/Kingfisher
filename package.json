{
  "name": "kingfisher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only audio file inspector and per-client/project work log.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "python3 -m http.server 8181",
    "test:browser": "node test/browser/make-fixtures.mjs && node test/browser/e2e.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0"
  }
}
